import React from "react";
import {Text, View} from "react-native";
import SQLite from 'react-native-sqlite-storage';

const Main = () => {
    return (
        <View>
            <Text>Este texto </Text>
        </View>
    );
};

export default Main;